// This project can run in free mode using the local AI assistant below.
// Leave OPENAI_API_KEY empty if you do not want to use OpenAI.
// If you add a valid OpenAI key, the chat will use it automatically.
window.OPENAI_API_KEY = "";
window.OPENAI_MODEL = "gpt-3.5-turbo"; // optional if using OpenAI
window.OPENAI_PROXY_URL = null; // Optional: set to your own server proxy URL if browser-side requests fail.
